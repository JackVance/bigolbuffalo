{
  "name": "bigolbuffalo",
  "version": "1.0.0",
  "private": true,
  "description": "bigolbuffalo.com — static site built with Eleventy",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "node -e \"require('fs').rmSync('_site', { recursive: true, force: true })\"",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
